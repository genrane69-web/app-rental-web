import streamlit as st

st.set_page_config(
    page_title="AppRental - บริการให้เช่าระบบเว็บแอปพร้อมใช้งาน",
    page_icon="🚀",
    layout="wide"
)

st.title("🚀 AppRental - เช่าระบบแอปองค์กร เริ่มต้นเพียงหลักร้อย/เดือน")
st.subheader("ยกระดับธุรกิจของคุณด้วยระบบแอปเปิ้ลแอปสำเร็จรูป ไม่ต้องลงทุนสร้างใหม่")
st.write("พร้อมใช้งานทันที ดูแลระบบให้ 24 ชม. ปรับแต่งตามแบรนด์ของคุณได้")

st.divider()

st.header("💡 บริการระบบแอปของเรา")

col1, col2 = st.columns(2)

with col1:
    st.markdown("### 🛒 1. ระบบจัดการหน้าร้าน & ขายของ (POS)")
    st.write("• บันทึกยอดขาย เช็คสต็อกสินค้าอัจฉริยะ")
    st.write("• พิมพ์ใบเสร็จ และสรุปรายงานยอดขาย daily/monthly")
    st.write("• ออกรายงานเข้า Google Sheets / Excel อัตโนมัติ")
    st.caption("💰 ค่าบริการเริ่มต้น: 299 บาท / เดือน")
    st.button("สนใจระบบ POS", key="pos")

with col2:
    st.markdown("### 🏢 2. ระบบบริหารจัดการหอพัก & อพาร์ทเม้นท์")
    st.write("• ออกใบแจ้งหนี้ ค่าน้ำ-ค่าไฟ อัตโนมัติ")
    st.write("• ระบบแจ้งซ่อมและติดตามสถานะสำหรับผู้เช่า")
    st.write("• ส่งการแจ้งเตือนค่าน้ำไฟเข้า LINE ผู้เช่าโดยตรง")
    st.caption("💰 ค่าบริการเริ่มต้น: 499 บาท / เดือน")
    st.button("สนใจระบบหอพัก", key="dorm")

st.divider()

st.header("⚡ ทำไมต้องเลือกเช่าระบบกับเรา?")

col_a, col_b, col_c = st.columns(3)
with col_a:
    st.markdown("#### 🚀 พร้อมใช้ใน 5 นาที")
    st.write("ไม่ต้องรอเขียนโปรแกรมเป็นเดือนๆ สมัครปุ๊บ ได้ลิงก์เข้าใช้งานทันที")
with col_b:
    st.markdown("#### 🛠️ ดูแลระบบฟรี")
    st.write("มีทีมงานดูแลเซิร์ฟเวอร์ อัปเดตฟีเจอร์ใหม่ และสำรองข้อมูลให้ตลอดเวลา")
with col_c:
    st.markdown("#### 📱 รองรับทุกอุปกรณ์")
    st.write("ใช้งานได้ทั้งบนมือถือ แท็บเล็ต และคอมพิวเตอร์ โดยไม่ต้องติดตั้งแอป")

st.divider()

st.header("📩 ลงชื่อรับสิทธิ์ทดลองใช้งานฟรี 14 วัน!")
st.write("กรอกข้อมูลไว้เพื่อรับข้อเสนอพิเศษช่วงเปิดตัว (ไม่มีข้อผูกมัด)")

with st.form("lead_form"):
    name = st.text_input("ชื่อ-นามสกุล หรือ ชื่อร้านค้า")
    email = st.text_input("อีเมล / เบอร์โทรศัพท์")
    selected_app = st.selectbox(
        "ระบบที่คุณสนใจมากที่สุด",
        ["ระบบขายของ (POS)", "ระบบจัดการหอพัก", "ระบบอื่นๆ (ต้องการให้พัฒนาเพิ่ม)"]
    )
    note = st.text_area("รายละเอียดเพิ่มเติม / สอบถามเพิ่มเติม")
    
    submit = st.form_submit_button("ส่งข้อมูลขอรับสิทธิ์ใช้งานฟรี 🚀", type="primary")

    if submit:
        if name and email:
            st.success(f"ขอบคุณครับคุณ {name}! เราได้รับข้อมูลเรียบร้อยแล้ว ทีมงานจะติดต่อกลับผ่าน {email} โดยเร็วที่สุดครับ")
        else:
            st.warning("กรุณากรอกชื่อและช่องทางการติดต่อให้ครบถ้วนนะครับ")
